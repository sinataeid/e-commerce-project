// AddToBasketButton.jsx
import React from 'react';

const AddToBasketButton = ({ onClick }) => (
  <div>
    <button onClick={onClick}>Add to Basket</button>
  </div>
);

export default AddToBasketButton;
